.venv/
__pycache__/
*.pyc
.DS_Store
.idea/
.vscode/

# Bí mật — KHÔNG BAO GIỜ commit
.streamlit/secrets.toml
.env
*.key

# Backtest: cache du lieu va ket qua chay
backtest/cache/
backtest/results/

# Cache nen noi phien — tai lai duoc tu nguon, khong phai ma nguon.
# Mot ma / mot khung ~2.200 nen mot nam; ca ro se rat nang neu commit.
intraday_cache/

# Ket qua chay hang ngay — trang thai chay, khong phai ma nguon.
reports/

# Sổ lệnh giấy — MỌI file .db đều là trạng thái chạy, không commit.
# Trước đây chỉ chặn `paper_custom*.db` nên `paper_trades.db` lọt qua và bị
# commit 16 lần; commit e2f98b4 đã ghi đè 96/113 lệnh thật bằng kết quả
# in-sample. Bản sạch cuối cùng nằm ở commit e9c5113.
*.db

# Bộ nhớ mẫu hình cắt lỗ — trạng thái chạy, phình ra sau mỗi lần chạy.
# Commit nó vào git là để một file 2 MB quyết định điểm số của agent.
sl_pattern_memory.json

# Kết quả và log chạy thử nghiệm
walkforward_results/
*.pkl
*.txt
*.log

# File nháp để soi lỗi bằng tay — không phải mã của dự án.
# Hook chan_bia_so_lieu.py cũng miễn trừ thư mục này.
scratch/
# Ban sao bang chung Phase 0 -- phai nam NGOAI repo (xem docs/STATE.md)
_phase0_snapshot.tar.gz
*.tar.gz
