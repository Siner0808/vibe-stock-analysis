[pytest]
# Chỉ thu test trong tests/. Không có dòng này thì `pytest` trần ở gốc repo
# thu — tức IMPORT, tức CHẠY mã mức module — ba script gốc khớp mẫu mặc
# định `test_*.py` / `*_test.py`. Hai trong số đó `os.remove()` hai file
# .db đo lường mà docs/kho-db-goc-repo.md xếp vào mục GIỮ.
# Audit 25/09/2026, docs/STATE.md BƯỚC 121, phát hiện ma_app_script-03.
# Khoá bởi tests/test_pytest_ini.py.
testpaths = tests
