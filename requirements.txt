# Liệt kê thứ cài được từ PyPI CÔNG KHAI — cộng HAI gói, `vnstock` và
# `vnai`, lấy từ kho riêng của hãng (khối "KHO THỨ HAI" bên dưới).
#
# GitHub Actions và Streamlit Cloud đều chạy `pip install -r requirements.txt`.
# Bốn gói tài trợ của vnstock — vnstock_data, vnstock_ta, vnstock_news,
# vnstock_pipeline — cùng `vnii` KHÔNG có trên PyPI công khai; chúng tải qua
# API có xác thực bằng khoá. Thêm chúng vào đây sẽ làm CI và cloud hỏng ngay
# ở bước cài, chứ không phải hỏng lúc chạy.
#
# Hệ quả phải biết: máy local có gói tài trợ nên chạy ở hạng silver
# (BCTC không giới hạn số kỳ, 300 req/phút); CI và Streamlit Cloud không có
# nên chạy ở hạng free (8 kỳ, 60 req/phút). `vnstock_goi.kiem_goi()` sẽ báo
# LỆCH trên cloud — đó là báo ĐÚNG, không phải lỗi cần sửa.
#
# Vì vậy KHÔNG mã nào ở gốc dự án được `import vnstock_data` ở mức module.
# Khoá bởi tests/test_requirements.py.
streamlit
pandas
numpy
plotly
# KHO THỨ HAI — từ 26/09/2026 (docs/STATE.md BƯỚC 127). PyPI cách ly
# `vnstock` và `vnai` từ tối 25/09; hãng phát hành trên kho riêng, và
# người dùng chốt cài từ đó. Đo 26/09: kho ấy chỉ phục vụ bốn tên —
# vnstock, vnstock-installer, vnai, vnii.
#
# GHIM ĐÚNG BẢN, không khai sàn: pip chọn bản cao nhất trên CẢ HAI kho,
# nên với một sàn thì kho nào có bản cao hơn quyết định CI chạy gì — và
# sàn cũng là cách CI từng chạy trước máy (lỗi 79). Bản ghim là bản ĐO 19
# đã đo ở máy; tests/test_requirements.py bắt hai bên khớp nhau.
#
# Bản cũ ở đây là `vnstock>=4.0.6` · `vnai>=2.5.7` — mức tối thiểu tài
# liệu Vibe Onboarding của vnstock yêu cầu (đọc 22/08/2026).
--extra-index-url https://vnstocks.com/api/simple
vnstock==4.0.9
vnai==2.6.2
tradingview-ta
feedparser
beautifulsoup4
requests
lxml
google-generativeai
gspread
toml
